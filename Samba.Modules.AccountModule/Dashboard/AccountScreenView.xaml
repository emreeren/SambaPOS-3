<UserControl x:Class="Samba.Modules.AccountModule.Dashboard.AccountScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="300">

    <UserControl.Resources>
        <DataTemplate x:Key="ItemTemplate">
            <TextBlock Text="{Binding Name}" />
        </DataTemplate>

        <DataTemplate x:Key="SelectedTemplate">
            <Grid FocusManager.FocusedElement="{Binding ElementName=cb}">
                <ComboBox Name="cb" Text="{Binding Name,UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Names}" IsEditable="True" HorizontalAlignment="Stretch"/>
            </Grid>
        </DataTemplate>

        <Style TargetType="{x:Type ListBoxItem}" x:Key="ContainerStyle">
            <Setter Property="ContentTemplate" Value="{StaticResource ItemTemplate}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="ContentTemplate" Value="{StaticResource SelectedTemplate}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Label>
                <LocText ResourceIdentifierKey="Name" Suffix=":"/>
            </Label>
            <TextBox HorizontalAlignment="Left" MinWidth="200" Text="{Binding Name}" />

            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="2,3" Text="{LocText AccountTemplate, Suffix=':'}"/>
                <TextBlock Margin="2,3">
                                        <Hyperlink Command="{Binding AddAccountTemplateNameCommand}">
                                            <TextBlock Text="{Binding Path=AddAccountTemplateNameCommand.Caption}" />
                                        </Hyperlink>
                                    </TextBlock>
                <TextBlock Margin="2,3">
                                        <Hyperlink Command="{Binding DeleteAccountTemplateNameCommand}">
                                            <TextBlock Text="{Binding Path=DeleteAccountTemplateNameCommand.Caption}" />
                                        </Hyperlink>
                                    </TextBlock>
            </StackPanel>
            <ListBox ItemsSource="{Binding AccountTemplateNamesList}" SelectedItem="{Binding SelectedAccountTemplateName}" 
                     HorizontalAlignment="Left" HorizontalContentAlignment="Stretch" MinWidth="200" MinHeight="100" ItemContainerStyle="{StaticResource ContainerStyle}">
            </ListBox>
            <Button HorizontalAlignment="Left" Margin="0,10" MinWidth="70" Content="{Binding SaveCommand.Caption}"
                 Command="{Binding SaveCommand}" />
            <Label Content="{Binding Error}" Foreground="Red" />
        </StackPanel>
    </Grid>
</UserControl>
