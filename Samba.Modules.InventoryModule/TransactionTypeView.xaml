<UserControl x:Class="Samba.Modules.InventoryModule.TransactionTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Label>
                <LocText ResourceIdentifierKey="Name" Suffix=":"/>
            </Label>
            <TextBox HorizontalAlignment="Left" MinWidth="200" Text="{Binding Name}" />
            <Label Content="{LocText Source_f, FormatSegment1={LocText ResourceType}, Suffix=':'}"/>
            <ComboBox HorizontalAlignment="Left" MinWidth="150" 
                      ItemsSource="{Binding ResourceTypes}" 
                      SelectedItem="{Binding SourceResourceType}" DisplayMemberPath="Name"/>
            <Label Content="{LocText Target_f, FormatSegment1={LocText ResourceType}, Suffix=':'}"/>
            <ComboBox HorizontalAlignment="Left" MinWidth="150" 
                      ItemsSource="{Binding ResourceTypes}" 
                      SelectedItem="{Binding TargetResourceType}" DisplayMemberPath="Name"/>
            <Label Content="{LocText Default_f, FormatSegment1={LocText Source_f, FormatSegment1=Resource}, Suffix=':'}"/>
            <ComboBox IsEditable="True" HorizontalAlignment="Left" MinWidth="150" 
                      ItemsSource="{Binding SourceResources}" 
                      SelectedValue="{Binding DefaultSourceResourceId}" SelectedValuePath="Id" DisplayMemberPath="Name"/>
            <Label Content="{LocText Default_f, FormatSegment1={LocText Target_f, FormatSegment1=Resource}, Suffix=':'}"/>
            <ComboBox IsEditable="True" HorizontalAlignment="Left" MinWidth="150" 
                      ItemsSource="{Binding TargetResources}" 
                      SelectedValue="{Binding DefaultTargetResourceId}" SelectedValuePath="Id" DisplayMemberPath="Name"/>
            <Label Content="{LocText AccountTransactionType, Suffix=':'}"/>
            <ComboBox IsEditable="True" HorizontalAlignment="Left" MinWidth="150" 
                      ItemsSource="{Binding AccountTransactionTypes}" 
                      SelectedItem="{Binding AccountTransactionType}" DisplayMemberPath="Name"/>

            <Button HorizontalAlignment="Left" Margin="0,10" MinWidth="70" Content="{Binding SaveCommand.Caption}"
                 Command="{Binding SaveCommand}" />
            <Label Content="{Binding Error}" Foreground="Red" />
        </StackPanel>
    </Grid>
</UserControl>
